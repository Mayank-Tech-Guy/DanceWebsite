git config --global user.name Mayank
git config --global user.email "cmayank006@gamil.com"
touch .gitignore
git init
git add .
git commit -m "Initial Commit"
git  status # Just to see
 ssh-keygen -t rsa -b 4096 -C "cmayank006@gmail.com"
tail <path to id_rsa.pab> # Add this key to your github account
git remote add origin <ssh url.git>
git push origin master
